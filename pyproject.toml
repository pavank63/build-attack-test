[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "build-attack-test"
version = "0.1.0"
description = "Security test package to validate build isolation"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
